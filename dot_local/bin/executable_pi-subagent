#!/bin/bash
set -euo pipefail

usage() {
  echo "usage: ${0##*/} <prompt>" >&2
}

case "${1:-}" in
-h | --help)
  usage
  exit 0
  ;;
esac

if [[ $# -ne 1 ]]; then
  usage
  exit 2
fi

pi --provider openai-codex --model gpt-5.6-luna -p "$1"
