eval "$(~/.local/bin/mise activate)"

export EDITOR=nvim
export VISUAL=nvim

alias lg='lazygit'
alias la='ls -a'
alias lingo='cd ~/Documents/GitHub/LearnLingo/'
alias c='claude --dangerously-skip-permissions'
alias vault='cd ~/Documents/Second\ Brain/'

chezmoi-clean() {
  chezmoi managed | while read -r f; do
    [[ ! -e "$HOME/$f" ]] && chezmoi forget "$HOME/$f"
  done
}


# opencode
export PATH=/Users/eddiehuang/.opencode/bin:$PATH
